# 调度与时序设计（保证“不卡、不漂、不抖”）

## 统一时间基准

- Timer0/Timer1 提供 1ms tick
- 所有任务使用软定时器和事件队列调度
- 严禁在主逻辑中使用长 delay（否则 UI、红外、步进都会抖/丢）

## 建议的周期任务（示例）

- every 1ms：键盘扫描、蜂鸣器节拍、步进电机步进脉冲
- every 10ms：UI刷新节流、红外解码推进
- every 200ms：温度采样（或更慢）
- every 1s：时间同步显示、在线状态统计、日志心跳（可选）
