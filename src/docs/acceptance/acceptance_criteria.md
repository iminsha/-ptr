# 验收标准（你做完就能“交付”的标准）

必须满足：

- 上电 3 秒内进入主界面并稳定刷新
- 时间正确可设置，掉电再上电仍正确（或可检测不可信并提示）
- 温度显示稳定，不抖动（不出现随机跳变/假数据）
- 设置阈值与日程后，掉电重启依旧有效
- 规则触发动作符合逻辑（含滞回与冷却时间）
- 步进电机可到达指定开度，并可手动停止
- 红外/按键任一方式能进行关键控制（至少一种输入可靠）
- 串口日志能输出：启动、配置加载结果、每次触发事件、错误与恢复
