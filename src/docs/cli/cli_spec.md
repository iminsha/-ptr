# 串口 CLI（可选但强烈推荐，便于验收）

命令集合建议：

- help
- status（时间/温度/模式/开度/错误码）
- task list/add/del
- rule show/set
- move <pct> stop home
- cfg save/load/default
- log tail/clear

注意：CLI 不要求花哨，关键是“能把配置导出/导入、能复现问题”。
