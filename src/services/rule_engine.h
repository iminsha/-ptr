/*
 * services/rule_engine.h
 *
 * 模块职责：
 * 1) 基于输入状态（温度、模式、时间等）执行规则判定。
 * 2) 输出统一动作建议（如 on_high / on_recover / noop）。
 * 3) 不直接操作硬件，只给出“应该执行什么动作”的决策结果。
 *
 * 设计原则：
 * - 与传输层/协议层解耦：不关心 TCP/串口来源。
 * - 与执行层解耦：不直接调用电机、蜂鸣器驱动。
 * - 可测试：相同输入应得到可预期输出。
 */

#pragma once
